#include <stdio.h>
// это какой то код на С
int main() {

    int summ_razn=0, razn=0, max_razn=0, n;
    scanf("%d", &n);
    int arr[n];
    for (int i=0; i<n-1; i++) {
        scanf("%d", &arr[i]);
}
}