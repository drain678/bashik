int main() {
    for (int i = 0; i < 5; i++){
        // pushFront(&head, i);
        pushback(i, i);
    }
    // Node *prelast = getprelast(head);
    // printf("%d\n", prelast->value);
    // popfront(&head);
    // print(head);
    // Node *last = getlast(head);
    // printf("%d\n", last->value);    34 
    return(0);
}